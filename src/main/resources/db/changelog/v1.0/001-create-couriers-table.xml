<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="1" author="yandex-lavka">

        <!-- 1. Создание таблицы -->
        <createTable tableName="couriers" schemaName="lavka_schema">
            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="courier_type" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>
            <column name="regions" type="INTEGER[]">
                <constraints nullable="false"/>
            </column>
            <column name="working_hours" type="TEXT[]">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- 2. Индекс по типу курьера -->
        <createIndex
                indexName="idx_courier_type"
                tableName="couriers"
                schemaName="lavka_schema">
            <column name="courier_type"/>
        </createIndex>

        <!-- 3. Индекс по времени создания -->
        <createIndex
                indexName="idx_courier_created_at"
                tableName="couriers"
                schemaName="lavka_schema">
            <column name="created_at"/>
        </createIndex>

    </changeSet>

</databaseChangeLog>